<div class="comment">
    {{#each this}}
    <section class="comment">
        {{comment_text}}
        <div class="meta">
            {{user.username}} on {{created_at}}
        </div>
    </section>
    {{/each}}
</div>